# === 1. Tu componente existente ===
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: backstage-app
  title: Backstage App
  description: Una aplicación que muestra todo el poder de Backstage.
  tags:
    - backend
    - python
  annotations:
    backstage.io/techdocs-ref: dir:.
    backstage.io/kubernetes-id: my-awesome-app
    github.com/project-slug: tu-org/my-awesome-app
spec:
  type: website
  owner: development
  lifecycle: production
  # providesApis:
  #   - my-awesome-api
  dependsOn:
    - component:default/python-app

---
# === 2. Declaración de ubicación que apunta a más componentes ===
apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: backstage-app-related-entities
  description: Entidades relacionadas a backstage-app
spec:
  type: file
  targets:
    - ./components/auth-module.yaml
